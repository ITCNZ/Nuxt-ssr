import Vue from 'vue'
import {Input, Button, Form, FormItem, Pagination} from 'element-ui'

export default () => {
  Vue.use(Input);
  Vue.use(Button);
  Vue.use(Form);
  Vue.use(FormItem);
  Vue.use(Pagination);
}
